import React from "react";
import "./Loader.css";

function Loader() {
  return (
    <span
      className="loader"
      //   style={{
      //     left: Left ? `${Left}` : "50%",
      //   }}
    ></span>
  );
}

export default Loader;
